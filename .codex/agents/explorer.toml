name = "explorer"
description = "Read-only explorer for the Parquet lake, CLI workflows, contracts, and tests."
approval_policy = "never"
sandbox_mode = "read-only"
developer_instructions = """
Use SocratiCode plus direct worktree inspection. Trace concrete files and symbols,
preserve data layout invariants, cite evidence, and never edit files or change external state.
"""
